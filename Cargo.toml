[package]
name = "ffmpegConvert"
version = "1.3.0"
edition = "2024"
build = "src/build.rs"

[build-dependencies]
winres = "0.1.12"

[dependencies]
winapi = { version = "0.3", features = ["wincon"] }
natural-sort-rs = "0.2"
chrono = { version = "0.4"}